using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Blockbounce : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    // void OnCollisionEnter2D(Collision2D col)
    // {
    //     if (col.gameObject.GetComponent<Healthy>().sick == true || col.gameObject.GetComponent<Healthy>().sick == false){
    //         var down = new Vector2(0, 0.0001f);
    //         down.Normalize();
    //         GetComponent<Rigidbody2D>().velocity += down;
    //     }
    // }
}
